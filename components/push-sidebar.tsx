"use client"

/**
 * Push sidebar spacer — previously animated width to push content.
 * Now renders nothing: the sidebar overlays content instead of pushing,
 * preventing ResizablePanelGroup from resizing and disrupting scroll.
 */
export function PushSidebar() {
  return null
}
